1) Download tungo and make it executable:
```bash
wget https://github.com/NLipatov/TunGo/releases/latest/download/tungo-linux-x64 -O tungo
chmod +x tungo
```

2) Move into your $PATH:
```bash
sudo mv tungo /usr/local/bin/
```

