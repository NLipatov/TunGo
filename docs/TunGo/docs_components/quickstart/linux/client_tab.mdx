import BrowserOnly from '@docusaurus/BrowserOnly';

# Client
1. Start the client:
```bash
sudo tungo
```

2. Add new configuration and use it to create a vpn-tunnel

## Recap:
<BrowserOnly fallback={<div>Loading...</div>}>
    {
        () => {
            const Player = require('@site/src/components/asciinema/player').default;
            return (
            <Player castPath="/asciinema/quickstart/client.cast" rows="30" cols="60" />
            );
        }
    }
</BrowserOnly>