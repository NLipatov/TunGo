# Servidor TunGo en Docker

**1️⃣ Preparar la imagen**
- 🐳 Oficial:
```bash
  docker pull nlipatov/tungo:latest
````

* 🛠 O constrúyela tú mismo:

  ```bash
  cd src/
  docker buildx build -t nlipatov/tungo:latest .
  ```

**2️⃣ Ejecutar el contenedor**

```bash
docker run -d \
  --name tungo \
  --restart always \
  --network host \
  --device /dev/net/tun \
  --cap-add NET_ADMIN \
  -e EnableUDP=true \
  -e EnableTCP=false \
  -v tungo_volume:/etc/tungo \
  nlipatov/tungo:latest
```

**3️⃣ Generar configuración del cliente**

```bash
docker exec tungo bash -c "tungo s gen"
```

> También puedes usar el ID del contenedor en lugar de su nombre.

📖 Consulta [Configuración del cliente](/docs/QuickStart#%EF%B8%8F-client-setup) para los siguientes pasos.
