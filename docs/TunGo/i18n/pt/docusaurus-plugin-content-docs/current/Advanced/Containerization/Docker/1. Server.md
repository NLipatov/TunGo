# Servidor TunGo no Docker

**1️⃣ Preparar a imagem**
- 🐳 Oficial:
```bash
  docker pull nlipatov/tungo:latest
````

* 🛠 Ou construa você mesmo:

  ```bash
  cd src/
  docker buildx build -t nlipatov/tungo:latest .
  ```

**2️⃣ Executar o container**

```bash
docker run -d \
  --name tungo \
  --restart always \
  --network host \
  --device /dev/net/tun \
  --cap-add NET_ADMIN \
  -e EnableUDP=true \
  -e EnableTCP=false \
  -v tungo_volume:/etc/tungo \
  nlipatov/tungo:latest
```

**3️⃣ Gerar configuração do cliente**

```bash
docker exec tungo bash -c "tungo s gen"
```

> Você também pode usar o ID do container em vez do nome.

📖 Consulte [Configuração do cliente](/docs/QuickStart#%EF%B8%8F-client-setup) para os próximos passos.
