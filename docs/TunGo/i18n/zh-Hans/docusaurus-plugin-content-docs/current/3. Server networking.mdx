---
id: ServerNetworking
---

# 服务端网络与端口

默认端口：**UDP — 9090**、**TCP — 8080**、**WebSocket — 1010**。新建的服务器配置默认启用这三种协议。

请确保客户端能访问所用端口，并在服务器和云服务商的防火墙中允许连接。

## 修改协议或端口

启用的协议和端口在 `/etc/tungo/server_configuration.json` 中配置。如果修改服务端端口，也请同步更新客户端配置中的端口。修改后，重启服务端和客户端。
