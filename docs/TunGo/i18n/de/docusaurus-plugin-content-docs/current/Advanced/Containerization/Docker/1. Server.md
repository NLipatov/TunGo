# TunGo-Server in Docker

**1️⃣ Image vorbereiten**
- 🐳 Offiziell:
```bash
  docker pull nlipatov/tungo:latest
````

* 🛠 Oder selbst bauen:

  ```bash
  cd src/
  docker buildx build -t nlipatov/tungo:latest .
  ```

**2️⃣ Container ausführen**

```bash
docker run -d \
  --name tungo \
  --restart always \
  --network host \
  --device /dev/net/tun \
  --cap-add NET_ADMIN \
  -e EnableUDP=true \
  -e EnableTCP=false \
  -v tungo_volume:/etc/tungo \
  nlipatov/tungo:latest
```

**3️⃣ Client-Konfiguration generieren**

```bash
docker exec tungo bash -c "tungo s gen"
```

> Sie können auch die Container-ID anstelle des Namens verwenden.

📖 Siehe [Client-Einrichtung](/docs/QuickStart#%EF%B8%8F-client-setup) für die nächsten Schritte.
