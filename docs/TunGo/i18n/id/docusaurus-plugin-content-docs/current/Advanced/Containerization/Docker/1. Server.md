# Server TunGo di Docker

**1️⃣ Siapkan image**
- 🐳 Resmi:
```bash
  docker pull nlipatov/tungo:latest
````

* 🛠 Atau bangun sendiri:

  ```bash
  cd src/
  docker buildx build -t nlipatov/tungo:latest .
  ```

**2️⃣ Jalankan container**

```bash
docker run -d \
  --name tungo \
  --restart always \
  --network host \
  --device /dev/net/tun \
  --cap-add NET_ADMIN \
  -e EnableUDP=true \
  -e EnableTCP=false \
  -v tungo_volume:/etc/tungo \
  nlipatov/tungo:latest
```

**3️⃣ Buat konfigurasi klien**

```bash
docker exec tungo bash -c "tungo s gen"
```

> Anda juga bisa menggunakan ID container sebagai pengganti namanya.

📖 Lihat [Pengaturan klien](/docs/QuickStart#%EF%B8%8F-client-setup) untuk langkah selanjutnya.
