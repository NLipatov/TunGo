# Сервер TunGo в Docker

**1️⃣ Подготовьте образ**
- 🐳 Официальный:
```bash
  docker pull nlipatov/tungo:latest
````

* 🛠 Или соберите самостоятельно:

  ```bash
  cd src/
  docker buildx build -t nlipatov/tungo:latest .
  ```

**2️⃣ Запустите контейнер**

```bash
docker run -d \
  --name tungo \
  --restart always \
  --network host \
  --device /dev/net/tun \
  --cap-add NET_ADMIN \
  -e EnableUDP=true \
  -e EnableTCP=false \
  -v tungo_volume:/etc/tungo \
  nlipatov/tungo:latest
```

**3️⃣ Сгенерируйте конфигурацию клиента**

```bash
docker exec tungo bash -c "tungo s gen"
```

> Вы также можете использовать ID контейнера вместо его имени.

📖 Смотрите [Настройка клиента](/docs/QuickStart#%EF%B8%8F-client-setup) для следующих шагов.
