error_log stderr;
pid /run/nginx.pid;
events {}
http {
    access_log /tmp/target.log;
    server {
        listen 198.18.0.2:8080;
        listen [fd73:7467:6f::2]:8080;
        root /var/www/localhost/htdocs;
        location = /peer { return 200 "$remote_addr\n"; }
    }
}
